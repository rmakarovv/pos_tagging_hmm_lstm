The following python notebook contains my solution for POS tagging using two methods:
1. Hidden Markov Model - Viterbi algorithm
2. LSTM

I have compared the performance of the models and showcased their mistakes.

Requirements:

    pip install nltk
    pip install numpy
    pip install torch
